# application.yml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  
  h2:
    console:
      enabled: true
  
  redis:
    host: localhost
    port: 6379
    timeout: 2000
  
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    publisher-confirms: true
    publisher-returns: true
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: 2017araj@gmail.com
    password: Ankit@123
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    com.notification: DEBUG
    org.springframework.amqp: DEBUG
